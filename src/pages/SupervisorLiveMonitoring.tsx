import React from 'react';
import { Typography } from '@mui/material';

export const SupervisorLiveMonitoring: React.FC = () => (
  <Typography variant="h4" gutterBottom>Supervisor Live Monitoring (placeholder)</Typography>
);
