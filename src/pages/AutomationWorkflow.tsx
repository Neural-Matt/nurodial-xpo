import React from 'react';
import { Typography } from '@mui/material';

export const AutomationWorkflow: React.FC = () => (
  <Typography variant="h4" gutterBottom>Automation Workflow (placeholder)</Typography>
);
