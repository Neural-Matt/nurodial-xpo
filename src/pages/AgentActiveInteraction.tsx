import React from 'react';
import { Typography } from '@mui/material';

export const AgentActiveInteraction: React.FC = () => (
  <Typography variant="h4" gutterBottom>Agent Active Interaction (placeholder)</Typography>
);
